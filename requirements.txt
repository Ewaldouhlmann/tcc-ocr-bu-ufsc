# Núcleo (sempre necessário)
pytesseract==0.3.13
Pillow==11.1.0
pdf2image==1.17.0
python-dotenv==1.0.1
jiwer==3.0.5

# EasyOCR
easyocr==1.7.2

# Kraken (requer Python <=3.11, comentado para Python 3.13)
# kraken==5.2.9

# PaddleOCR (instale separado — depende do PaddlePaddle)
# pip install paddlepaddle paddleocr
# paddleocr==2.9.1

# DocTR (instale separado — escolha o backend; usamos torch)
# pip install "python-doctr[torch]"
# python-doctr==0.9.0

# TrOCR (Transformer) + segmentação de linhas
transformers>=4.46
torch>=2.5
craft-text-detector>=0.4.3

# VLMs locais (Gemma 4, DeepSeek-VL2, Qwen2.5-VL) via Ollama
ollama>=0.4

# VLMs via API
llama-parse>=0.5
mistralai>=1.2
google-generativeai>=0.8
