# Núcleo (sempre necessário)
pytesseract==0.3.13
Pillow==11.1.0
pdf2image==1.17.0
python-dotenv==1.0.1
jiwer==3.0.5
openpyxl==3.1.5
pyyaml==6.0.2

# EasyOCR
easyocr==1.7.2

# Kraken (requer Python <=3.11, comentado para Python 3.13)
# kraken==5.2.9

# PaddleOCR (CPU; troque por paddlepaddle-gpu se houver CUDA)
# ATENÇÃO: paddleocr instala opencv-python e opencv-contrib-python além do
# opencv-python-headless já usado por easyocr/doctr; ter as 3 variantes ao
# mesmo tempo quebra "import cv2" (AttributeError em cv2.dnn.DictValue). Após
# instalar este arquivo, rode:
#   pip uninstall -y opencv-python opencv-contrib-python opencv-python-headless
#   pip install opencv-python-headless==4.11.0.86
paddlepaddle==2.6.2
paddleocr==2.9.1

# DocTR (backend PyTorch)
python-doctr[torch]==1.0.1

# TrOCR (Transformer) + segmentação de linhas
transformers>=4.46
torch>=2.5
craft-text-detector>=0.4.3

# VLMs locais (Gemma 4, DeepSeek-VL2, Qwen2.5-VL) via Ollama
ollama>=0.4

# VLMs via API
llama-parse>=0.5
mistralai>=1.2
google-generativeai>=0.8
